'use client';
import {useState} from 'react';
import {ContactLink} from './ContactLink';
export function Header(){const [open,setOpen]=useState(false);return <header className="header"><div className="shell header-inner"><a className="brand" href="#inicio" aria-label="Dra. Melissa Randazzo, início">Melissa Randazzo<span>ODONTOLOGIA · RONDONÓPOLIS</span></a><button className="menu-toggle" aria-label={open?'Fechar menu':'Abrir menu'} aria-expanded={open} aria-controls="navigation" onClick={()=>setOpen(!open)}>{open?'Fechar ✕':'Menu ☰'}</button><nav id="navigation" className={open?'navigation is-open':'navigation'} aria-label="Navegação principal" onKeyDown={e=>{if(e.key==='Escape')setOpen(false)}}>{[['servicos','Serviços'],['sobre','Sobre'],['avaliacoes','Avaliações'],['localizacao','Localização'],['contato','Contato']].map(([id,label])=><a key={id} href={'#'+id} onClick={()=>setOpen(false)}>{label}</a>)}<ContactLink placement="header" className="button button-small">Agendar consulta</ContactLink></nav></div></header>}
