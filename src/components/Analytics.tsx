'use client';
import {useEffect,useState} from 'react';
import Script from 'next/script';
export function Analytics(){const [consent,setConsent]=useState<string|null>('pending');const [show,setShow]=useState(false);const id=process.env.NEXT_PUBLIC_GTM_ID;useEffect(()=>{try{setConsent(localStorage.getItem('analytics-consent'));}catch{setConsent(null)}},[]);function choose(value:string){try{localStorage.setItem('analytics-consent',value)}catch{}setConsent(value);setShow(false);if(value==='denied')window.location.reload();}if(!id||!/^GTM-[A-Z0-9]+$/.test(id))return null;return <>{consent==='granted'&&<Script id="gtm" strategy="afterInteractive">{`(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],j=d.createElement(s);j.async=true;j.src='https://www.googletagmanager.com/gtm.js?id='+i;f.parentNode.insertBefore(j,f);})(window,document,'script','dataLayer','${id}');`}</Script>}{(consent===null||show)?<aside className="consent" aria-label="Preferências de privacidade"><p>Podemos usar análise de navegação para melhorar este site? Sua escolha não altera o atendimento.</p><button onClick={()=>choose('granted')}>Aceitar análise</button><button onClick={()=>choose('denied')}>Somente necessários</button></aside>:<button className="privacy-settings" onClick={()=>setShow(true)}>Preferências de privacidade</button>}</>}
